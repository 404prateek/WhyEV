# Assets Directory

This directory contains static branding assets, icons, and media files used across the WhyEV application.
- Public images & logos are hosted in `/public` for static web serving.
- SVGs, icons, and graphic components are maintained here.
